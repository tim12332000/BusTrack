# 50 臺遊覽車上車進度管理方案 (Google Form + Google Sheet)

## 一、可行性分析

**結論：完全可行，且是成本最低、最穩定的敏捷方案！**

- **高併發承載力**：Google Forms 每秒可承載數百次提交，50 台車長同時提交完全不會卡頓。
- **免裝 App**：車長只需用 LINE/微信/簡訊點開連結即可填報。
- **即時視覺化**：Google Sheet 可透過 `XLOOKUP` 反向查詢最新一筆回報，配合條件格式化（紅黃綠色），在主控室大螢幕即時投影 50 台車最新狀態。

---

## 二、3分鐘最小測試 (MVP) 實作步驟

### 步驟 1：建立 Google 表單 (Google Form)
1. 前往 [Google Forms](https://forms.google.com/) 建立新表單，命名為 `【遊覽車上車進度回報】`。
2. 建立以下欄位（**關鍵：車號務必用下拉選單，避免手打打錯**）：
   - **車號**（下拉式選單）：`01車`、`02車`、`03車` ... 至 `50車` （必填）
   - **車長姓名**（簡答文字）（選填/必填）
   - **應到人數**（簡短數字，若每車固定可不填）（選填）
   - **實到人數**（簡短數字，設定為大於等於0的整數）（必填）
   - **目前狀態**（單選題）：（必填）
     - 🟡 上車中
     - 🟢 全員到齊
     - 🚀 已出發
   - **備註/缺席原因**（段落文字）（選填）

---

### 步驟 2：連結到 Google 試算表 (Google Sheet)
1. 在 Google 表單點選「**回覆 (Responses)**」分頁。
2. 點擊「**建立試算表 (Link to Sheets)**」。
3. 此時會產生一個名為 `表單回應 1` 的工作表，欄位如下：
   - `A欄`: 時間戳記
   - `B欄`: 車號
   - `C欄`: 車長姓名
   - `D欄`: 應到人數
   - `E欄`: 實到人數
   - `F欄`: 目前狀態
   - `G欄`: 備註

---

### 步驟 3：在試算表中建立「即時戰情儀表板 (Dashboard)」
點擊試算表左下角 `+` 新增一個工作表，命名為 `即時看板`，設定如下：

#### 1. 頂部 KPI 統計區（第 1 ~ 2 列）：
| A1 | B1 | C1 | D1 | E1 |
|---|---|---|---|---|
| 總車數 | 已出發 (輛) | 全員到齊 (輛) | 上車中 (輛) | 尚未回報 (輛) |
| `50` | `=COUNTIF(D5:D54, "🚀 已出發")` | `=COUNTIF(D5:D54, "🟢 全員到齊")` | `=COUNTIF(D5:D54, "🟡 上車中")` | `=COUNTIF(D5:D54, "⚪ 尚未回報")` |

#### 2. 車輛明細清單（第 4 列開始為標題，第 5 ~ 54 列為 50 台車）：
- **A4: 車號**
- **B4: 應到人數**
- **C4: 最新實到人數**
- **D4: 最新狀態**
- **E4: 車長**
- **F4: 最後回報時間**
- **G4: 備註**

#### 3. 填入以下自動更新公式（以第 5 列 `01車` 為例）：
- **A5**：`01車`（A5:A54 填入 01車 ~ 50車）
- **B5**（應到人數）：`40`（依每車實際名額填入）
- **C5**（最新實到人數）：
  ```excel
  =XLOOKUP(A5, '表單回應 1'!B:B, '表單回應 1'!E:E, 0, 0, -1)
  ```
- **D5**（最新狀態）：
  ```excel
  =XLOOKUP(A5, '表單回應 1'!B:B, '表單回應 1'!F:F, "⚪ 尚未回報", 0, -1)
  ```
- **E5**（車長姓名）：
  ```excel
  =XLOOKUP(A5, '表單回應 1'!B:B, '表單活動 1'!C:C, "-", 0, -1)
  ```
- **F5**（最後回報時間）：
  ```excel
  =XLOOKUP(A5, '表單回應 1'!B:B, '表單回應 1'!A:A, "-", 0, -1)
  ```
- **G5**（備註）：
  ```excel
  =XLOOKUP(A5, '表單回應 1'!B:B, '表單回應 1'!G:G, "-", 0, -1)
  ```
*(將第 5 列公式向下複製到第 54 列即可！)*

> **公式核心原理**：`XLOOKUP(..., search_mode: -1)` 代表**由下往上（從最新資料）**搜尋，因此車長多次回報時，儀表板永遠只會顯示**最新一筆進度**！

---

## 三、加分建議與注意事項

1. **條件格式化 (高亮顏色)**：
   - 選取 D5:D54（狀態欄）：
     - 若包含 `已出發` → 填滿藍色
     - 若包含 `全員到齊` → 填滿綠色
     - 若包含 `上車中` → 填滿黃色
     - 若包含 `尚未回報` → 填滿灰色
2. **防呆機制**：
   - 車號務必使用**下拉選單**，避免手打 `01車`、`1車`、`第一車` 導致比對失敗。
3. **現場快速存取**：
   - 將 Google Form 網址轉成 **QR Code**，印在車長名牌或貼在每台車擋風玻璃上，車長手機一掃即開。
