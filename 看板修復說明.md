# 總即時戰情看板修復

## 套用

### 停車場改為直接回報剩餘車位

更新程式後，第一次選 **`upgradeParkingRemaining`** 執行；之後日常修復仍用 `repairDashboard`。

- 沿用原停車場表單網址，改為下列七處實際場地，名稱前的 `*` 保留。
- 題目為區域、目前剩餘汽車停車位、目前剩餘機車停車位、備註。
- 汽車與機車皆必填 0 或正整數；0 代表無空位，不需輸入進場／離場。
- 每場讀取最後一筆回報，不累加。未回報顯示「未回報」；有提供該車種的場地尚未齊全時，該車種總數顯示「未完整回報」。無該車種以「—」顯示，不影響完整性；有車位但停滿顯示 0。
- 新增剩餘車位題目，不將舊增減數量題目改名。舊回應資料留在試算表，不作為剩餘車位使用。
- 遷移若遇到回應標題尚未同步，稍後執行 `repairDashboard` 即可，無需重新建立表單。
- 必須同步更新兩份 HTML；新版 HTML 遇到舊版試算表會顯示等待更新，避免把「已停」誤讀為「剩餘」。

使用者確認以下數字為**總容量**，不會用來初始化即時剩餘車位。回報超出容量會顯示異常，不計入完整總數。

| 場地 | 機車總容量 | 汽車總容量 |
| --- | ---: | ---: |
| 五都日出 | 168 | 1008 |
| 新烏日 | 352 | 266 |
| *嶺東科大 | 800 | 90 |
| *台中科大 | 200 | 0 |
| 水湳轉運站 | 1253 | 614 |
| *經貿六 | 0 | 563 |
| *經貿八 | 0 | 482 |
| 合計 | 2773 | 3023 |

表單無該車種的欄位填 0，看板改以「—」表示。舊 A～F 回應不會猜測對應至這七處場地，需重新回報。

### 一般看板修復

1. 在目前試算表開啟「擴充功能 → Apps Script」。
2. 用 `【請複製這個】GoogleAppsScript完整終極版.js` 的完整內容覆蓋原本對應的程式檔，避免在其他檔案重複宣告相同常數。
3. 確認 `TARGET_SPREADSHEET_ID` 是目前使用的試算表 ID。
4. 選擇 **`repairDashboard`** 執行。此入口不新增或修改 Google 表單，不刪除原始回應資料；會重建總看板版面、公式及各車明細。
5. 查看執行紀錄的人員與停車場來源分頁。若有多個同類分頁且無法唯一識別，程式會在寫入看板前停止。把 `RESPONSE_SHEET_NAMES` 的 `people`、`parking` 填為實際接收目前表單回應的分頁名稱後重跑。不要只依名稱尾數判斷，可用最新回應時間及題目標題確認。
6. 各提交一筆可辨識的人員／停車場回報，確認回應分頁新增一列，對應看板數字更新。各車明細維持「同站、同車、同方向最新回報」；步行維持累加；停車場取各區最新汽機車剩餘格數。
7. 若使用網頁大螢幕，將更新後的 `index.html`／`戰情大螢幕.html` 套用至實際網站並重新整理。

## 修正原因

- 舊版以分頁順序找人員資料，只排除中文「回應 2」，會將英文 `Form Responses 2` 誤判為人員回應。
- 停車場以最多資料列選來源，可能讀到已停用的舊分頁。現在先比對必要題目標題，多候選時優先唯一仍連結表單的分頁；仍有歧義便要求指定。
- 人員欄位以前固定讀 B～E，並直接刪除多出的欄位。現在按標題定位，不刪除資料；重複標題會明確報錯。
- 缺少總看板時，以前會將第一個分頁改名再清空，現在只建立新的看板分頁。
- 網頁原先固定讀 `gid=0`，現在指定「總即時戰情看板」名稱；讀取失敗時顯示錯誤狀態。
- 看板清除範圍原先至少取 30 欄，可能超過實際欄數並隱藏錯誤，導致重跑卡在合併儲存格；現在使用實際大小並回報失敗。

## 驗證與限制

本機驗證：`node --test tests/dashboard.test.cjs tests/parking-html.test.cjs`，另做 JavaScript 語法與 `git diff --check` 檢查。專案未配置 lint／typecheck 工具，不新增依賴。

本機測試使用 Apps Script 模擬介面，未在 Google 雲端執行，也未驗證真實試算表公式計算結果。既有已被刪除的回應資料不會自動復原，必要時需從試算表版本紀錄找回。網頁既有 Gviz 列號推斷尚未改寫，若試算表已更新但網頁數字仍不一致，需另外檢查實際 Gviz 回傳。

來源辨識採用 Google 官方 [Sheet.getFormUrl 文件](https://developers.google.com/apps-script/reference/spreadsheet/sheet#getFormUrl()) 所定義的表單連結資訊。
